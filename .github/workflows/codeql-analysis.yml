name: CodeQL

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '32 14 * * 1'
  workflow_dispatch:

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'cpp' ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install packages
        run: >
          sudo apt --assume-yes install \
            cmake libbluetooth-dev clang-tidy cppcheck

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v1
        with:
          languages: ${{ matrix.language }}

      - name: Check project
        run: |
          ls -lha /home/runner/work/bose-connect-app-linux/bose-connect-app-linux/src/ || true
          ls -lha /home/runner/work/bose-connect-app-linux/bose-connect-app-linux/src/_lgtm_build_dir || true
          find / -iname *clang-tidy* || true
          whereis clang-tidy || true

      - name: Autobuild
        uses: github/codeql-action/autobuild@v1
        # ℹ️ Command-line programs to run using the OS shell.
        # 📚 https://git.io/JvXDl
        # ✏️ If the Autobuild fails above, remove it and uncomment the following three lines
        #    and modify them (or add more) to build your code if your project
        #    uses a compiled language
        #- run: |
        #   make bootstrap
        #   make release

      - name: Perform analysis
        uses: github/codeql-action/analyze@v1
        with:
          add-snippets: true
